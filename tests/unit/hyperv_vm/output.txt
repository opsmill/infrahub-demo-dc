# Auto-generated by Infrahub - do not edit by hand.
# VM: fra1-vm-win02
# Host: fra1-hv-01
# Cluster: FRA1-HYPERV-CLUSTER (hyperv)
$ErrorActionPreference = "Stop"

$TemplateVhdx = "C:\HyperV\Templates\tpl-windows-server-2022.vhdx"
$VmRoot = "C:\HyperV\VMs\fra1-vm-win02"
if (-not (Test-Path $TemplateVhdx)) {
    throw "Template $TemplateVhdx not found"
}
New-Item -ItemType Directory -Force -Path $VmRoot | Out-Null

# Cloud-init user-data rendered by Infrahub (see the vm-userdata artifact).
$UserData = @'
#cloud-config
# VM: fra1-vm-win02 (Windows guest - cloudbase-init)
set_hostname: fra1-vm-win02
users:
  - name: Admin
    groups: Administrators
    ssh_authorized_keys:
      - ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAA demo@opsmill
# NOTE: no password is stored in Infrahub. Set the first-boot Administrator
# password at deploy time (cloudbase-init inject or console).
'@
$MetaData = @"
instance-id: fra1-vm-win02
local-hostname: fra1-vm-win02
"@

$SeedDir = Join-Path $VmRoot "seed"
New-Item -ItemType Directory -Force -Path $SeedDir | Out-Null
Set-Content -Path (Join-Path $SeedDir "user-data") -Value $UserData -Encoding ascii -NoNewline
Set-Content -Path (Join-Path $SeedDir "meta-data") -Value $MetaData -Encoding ascii -NoNewline
& oscdimg.exe -j2 -lCIDATA $SeedDir (Join-Path $VmRoot "seed.iso")

Copy-Item $TemplateVhdx (Join-Path $VmRoot "fra1-vm-win02.vhdx")
Resize-VHD -Path (Join-Path $VmRoot "fra1-vm-win02.vhdx") -SizeBytes 120GB

New-VM -Name "fra1-vm-win02" `
    -MemoryStartupBytes 16GB `
    -Generation 2 `
    -VHDPath (Join-Path $VmRoot "fra1-vm-win02.vhdx") `
    -SwitchName "Default Switch"
Set-VMProcessor -VMName "fra1-vm-win02" -Count 4
Add-VMDvdDrive -VMName "fra1-vm-win02" -Path (Join-Path $VmRoot "seed.iso")

Start-VM -Name "fra1-vm-win02"
