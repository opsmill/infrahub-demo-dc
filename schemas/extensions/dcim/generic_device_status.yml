# yaml-language-server: $schema=https://schema.infrahub.app/infrahub/schema/latest.json
---
version: "1.0"

extensions:
  nodes:
    - kind: DcimGenericDevice
      attributes:
        - name: status
          kind: Dropdown
          optional: false
          order_weight: 1100
          choices:
            - name: active
              label: Active
              description: Fully operational and currently in service.
              color: "#7fbf7f"
            - name: provisioning
              label: Provisioning
              description: Being set up and configured.
              color: "#ffff7f"
            - name: maintenance
              label: Maintenance
              description: Undergoing routine maintenance or repairs.
              color: "#ffd27f"
            - name: drained
              label: Drained
              description: Temporarily taken out of service.
              color: "#bfbfbf"
